[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "rover-lib"
version = "0.1.0"
description = "Uma biblioteca Python para manipulação de um Rover com Visão Computacional."
readme = { file = "README.md", content-type = "text/markdown" }
requires-python = ">=3.9"

authors = [
  { name = "sarmento", email = "sarmento.neto.francisco@gmail.com" }
]

license = { file = "LICENSE" }

classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Operating System :: POSIX :: Linux",
  "Development Status :: 3 - Alpha",
  "Topic :: Scientific/Engineering :: Robotics",
]

dependencies = [
  "numpy",
  "opencv-python",
  "click"
]

[project.optional-dependencies]
cli = ["click"]
dev = ["pytest", "black", "ruff"]

[project.scripts]
rover = "rover.cli.main:cli"

[project.urls]
Homepage = "https://github.com/Rover-Project/Rover"
Repository = "https://github.com/Rover-Project/Rover"

[tool.setuptools]
package-dir = { "" = "src" }
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]

[project.entry-points."rover.camera"]
webcam = "rover.plugins.camera.webcam:Webcam"
picamera2 = "rover.plugins.camera.picamera2:PiCamera2"

[project.entry-points."rover.movement"]
l298n = "rover.plugins.movement.gpio_l298n:L298NMotor"
mock = "rover.plugins.movement.mock:MockMotor"
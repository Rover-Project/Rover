# Configuração para o script folowCircle
camera:
  fps: 30
  brigh: 0.5
  saturation: 1
  contrast: 1

gpio:
  motor:
    right: [15, 16] # in3, in4
    left: [12, 11] # in1, in2
    calibration:
      right: 0.6
      left: 1